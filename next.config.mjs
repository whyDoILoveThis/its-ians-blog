/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'img.clerk.com', 'its-portfolio.appspot.com'],
      },
};

export default nextConfig;
